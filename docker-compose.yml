services:
  api-gateway:
    build:
      context: ./api-gateway
      dockerfile: Dockerfile
    ports:
    - "8000:80"

  layout-analysis:
    build:
      context: ./layout-analysis
      dockerfile: Dockerfile
    ports:
    - "8001:80"
      
  logo-redaction:
    build:
      context: ./logo-redaction
      dockerfile: Dockerfile
    ports:
      - "8002:80"